{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "definitions": {
    "BlocklistConfig": {
      "additionalProperties": true,
      "properties": {
        "sources": {
          "description": "Array of blocklist sources, each with its own name, URL and cron schedule.",
          "items": {
            "additionalProperties": true,
            "properties": {
              "cron_schedule": {
                "description": "Cron spec for reloading this specific blocklist. If omitted, the blocklist is not scheduled for automatic updates.",
                "type": "string"
              },
              "description": {
                "description": "Optional description for the blocklist.",
                "type": "string"
              },
              "name": {
                "description": "Human-readable name for the blocklist (replaces the auto-generated 'Blocklist #N').",
                "type": "string"
              },
              "title": {
                "description": "Optional title for the blocklist.",
                "type": "string"
              },
              "url": {
                "description": "URL of the blocklist source.",
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "BlocklistSource": {
      "additionalProperties": true,
      "properties": {
        "cron_schedule": {
          "description": "Cron spec for reloading this specific blocklist. If omitted, the blocklist is not scheduled for automatic updates.",
          "type": "string"
        },
        "description": {
          "description": "Optional description for the blocklist.",
          "type": "string"
        },
        "name": {
          "description": "Human-readable name for the blocklist (replaces the auto-generated 'Blocklist #N').",
          "type": "string"
        },
        "title": {
          "description": "Optional title for the blocklist.",
          "type": "string"
        },
        "url": {
          "description": "URL of the blocklist source.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "CacheConfig": {
      "additionalProperties": true,
      "properties": {
        "cron_schedule": {
          "description": "Cron spec for cache reaper.",
          "type": "string"
        },
        "max_size": {
          "description": "Maximum number of entries in the DNS cache.",
          "type": "integer"
        },
        "ttl_floor": {
          "description": "Minimum TTL for cached entries.",
          "format": "duration",
          "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
          "type": "string"
        }
      },
      "type": "object"
    },
    "Config": {
      "additionalProperties": true,
      "properties": {
        "blocklist": {
          "additionalProperties": true,
          "properties": {
            "sources": {
              "description": "Array of blocklist sources, each with its own name, URL and cron schedule.",
              "items": {
                "additionalProperties": true,
                "properties": {
                  "cron_schedule": {
                    "description": "Cron spec for reloading this specific blocklist. If omitted, the blocklist is not scheduled for automatic updates.",
                    "type": "string"
                  },
                  "description": {
                    "description": "Optional description for the blocklist.",
                    "type": "string"
                  },
                  "name": {
                    "description": "Human-readable name for the blocklist (replaces the auto-generated 'Blocklist #N').",
                    "type": "string"
                  },
                  "title": {
                    "description": "Optional title for the blocklist.",
                    "type": "string"
                  },
                  "url": {
                    "description": "URL of the blocklist source.",
                    "type": "string"
                  }
                },
                "type": "object"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "dns": {
          "additionalProperties": true,
          "properties": {
            "cache": {
              "additionalProperties": true,
              "properties": {
                "cron_schedule": {
                  "description": "Cron spec for cache reaper.",
                  "type": "string"
                },
                "max_size": {
                  "description": "Maximum number of entries in the DNS cache.",
                  "type": "integer"
                },
                "ttl_floor": {
                  "description": "Minimum TTL for cached entries.",
                  "format": "duration",
                  "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "ecs": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "description": "Whether to enable EDNS0 Client Subnet (ECS) forwarding.",
                  "type": "boolean"
                }
              },
              "type": "object"
            },
            "noise_filter": {
              "additionalProperties": true,
              "properties": {
                "cron_schedule": {
                  "description": "Cron spec for noise filter downloader.",
                  "type": "string"
                },
                "url": {
                  "description": "URL of noise filter list (CSV format: category,rcode,domain_suffix).",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "timeouts": {
              "additionalProperties": true,
              "properties": {
                "dial": {
                  "description": "Timeout for establishing connections to upstream servers.",
                  "format": "duration",
                  "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                "read": {
                  "description": "Timeout for reading upstream DNS queries.",
                  "format": "duration",
                  "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                },
                "write": {
                  "description": "Timeout for writing upstream DNS queries.",
                  "format": "duration",
                  "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "upstreams": {
              "description": "Upstream DNS resolvers to forward queries to.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "geoblock": {
          "additionalProperties": true,
          "properties": {
            "ipinfo": {
              "additionalProperties": true,
              "properties": {
                "cron_schedule": {
                  "description": "Cron spec for Ipinfo.io database downloader.",
                  "type": "string"
                },
                "enabled": {
                  "description": "Whether to enable IPinfo.io geolocation lookups.",
                  "type": "boolean"
                },
                "token": {
                  "description": "IPInfo.io API token for downloading geoIP locations.",
                  "type": "string"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "server": {
          "additionalProperties": true,
          "properties": {
            "api_keys": {
              "description": "Map of API keys to user descriptions for admin API access.",
              "patternProperties": {
                ".*": {
                  "type": "string"
                }
              },
              "type": "object"
            },
            "data_dir": {
              "description": "Directory for storing persistent data (e.g., TLS certificate cache).",
              "type": "string"
            },
            "dev_mode": {
              "description": "Run server in dev mode (no TLS, plain TCP).",
              "type": "boolean"
            },
            "dns_port": {
              "description": "The port to run regular DNS (UDP/TCP) server on.",
              "type": "integer"
            },
            "dot_port": {
              "description": "The port to run DNS-over-TLS server on.",
              "type": "integer"
            },
            "http_port": {
              "description": "The port to run HTTP server on.",
              "type": "integer"
            },
            "lets_encrypt": {
              "additionalProperties": true,
              "properties": {
                "allowed_hosts": {
                  "description": "List of domains used for CertManager allow policy / mobileconfig.",
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "cloudflare_api_token": {
                  "description": "Cloudflare API token for DNS-01 challenge.",
                  "type": "string"
                },
                "email": {
                  "description": "Email address for Let's Encrypt registration.",
                  "type": "string"
                },
                "enabled": {
                  "description": "Enable automatic TLS certificate management via Let's Encrypt / ACME.",
                  "type": "boolean"
                }
              },
              "type": "object"
            },
            "log_level": {
              "description": "The logging level (DEBUG, INFO, WARN, ERROR).",
              "enum": [
                "DEBUG",
                "INFO",
                "WARN",
                "ERROR"
              ],
              "type": "string"
            },
            "proxy_protocol": {
              "additionalProperties": true,
              "properties": {
                "enabled": {
                  "description": "Require PROXY protocol header for DoT connections.",
                  "type": "boolean"
                },
                "trusted_proxies": {
                  "description": "Comma-separated list of trusted proxy IP addresses or CIDR ranges.",
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        },
        "telemetry": {
          "additionalProperties": true,
          "properties": {
            "metrics_auth": {
              "description": "Credentials for basic auth on /metrics (format: user:pass).",
              "type": "string"
            },
            "otel_endpoint": {
              "description": "OpenTelemetry OTLP gRPC endpoint (e.g. localhost:4317).",
              "type": "string"
            },
            "otel_sampling_ratio": {
              "description": "Ratio of traces to sample (0.0 to 1.0).",
              "type": "number"
            },
            "sentry_dsn": {
              "description": "DSN for Sentry error reporting.",
              "type": "string"
            },
            "top_k": {
              "additionalProperties": true,
              "description": "Configuration for the number of top entries to track in Prometheus metrics.",
              "properties": {
                "num_blocked": {
                  "description": "Number of top blocked domains to track in the dns_top_blocked_domains Prometheus metric.",
                  "type": "integer"
                },
                "num_clients": {
                  "description": "Number of top clients to track in the dns_top_clients Prometheus metric.",
                  "type": "integer"
                },
                "num_domains": {
                  "description": "Number of top (non-blocked) domains to track in the dns_top_domains Prometheus metric.",
                  "type": "integer"
                }
              },
              "type": "object"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "DNSConfig": {
      "additionalProperties": true,
      "properties": {
        "cache": {
          "additionalProperties": true,
          "properties": {
            "cron_schedule": {
              "description": "Cron spec for cache reaper.",
              "type": "string"
            },
            "max_size": {
              "description": "Maximum number of entries in the DNS cache.",
              "type": "integer"
            },
            "ttl_floor": {
              "description": "Minimum TTL for cached entries.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            }
          },
          "type": "object"
        },
        "ecs": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "description": "Whether to enable EDNS0 Client Subnet (ECS) forwarding.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "noise_filter": {
          "additionalProperties": true,
          "properties": {
            "cron_schedule": {
              "description": "Cron spec for noise filter downloader.",
              "type": "string"
            },
            "url": {
              "description": "URL of noise filter list (CSV format: category,rcode,domain_suffix).",
              "type": "string"
            }
          },
          "type": "object"
        },
        "timeouts": {
          "additionalProperties": true,
          "properties": {
            "dial": {
              "description": "Timeout for establishing connections to upstream servers.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            "read": {
              "description": "Timeout for reading upstream DNS queries.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            "write": {
              "description": "Timeout for writing upstream DNS queries.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            }
          },
          "type": "object"
        },
        "upstreams": {
          "description": "Upstream DNS resolvers to forward queries to.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ECSConfig": {
      "additionalProperties": true,
      "properties": {
        "enabled": {
          "description": "Whether to enable EDNS0 Client Subnet (ECS) forwarding.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "GeoblockConfig": {
      "additionalProperties": true,
      "properties": {
        "ipinfo": {
          "additionalProperties": true,
          "properties": {
            "cron_schedule": {
              "description": "Cron spec for Ipinfo.io database downloader.",
              "type": "string"
            },
            "enabled": {
              "description": "Whether to enable IPinfo.io geolocation lookups.",
              "type": "boolean"
            },
            "token": {
              "description": "IPInfo.io API token for downloading geoIP locations.",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "IpinfoConfig": {
      "additionalProperties": true,
      "properties": {
        "cron_schedule": {
          "description": "Cron spec for Ipinfo.io database downloader.",
          "type": "string"
        },
        "enabled": {
          "description": "Whether to enable IPinfo.io geolocation lookups.",
          "type": "boolean"
        },
        "token": {
          "description": "IPInfo.io API token for downloading geoIP locations.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "LetsEncryptConfig": {
      "additionalProperties": true,
      "properties": {
        "allowed_hosts": {
          "description": "List of domains used for CertManager allow policy / mobileconfig.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cloudflare_api_token": {
          "description": "Cloudflare API token for DNS-01 challenge.",
          "type": "string"
        },
        "email": {
          "description": "Email address for Let's Encrypt registration.",
          "type": "string"
        },
        "enabled": {
          "description": "Enable automatic TLS certificate management via Let's Encrypt / ACME.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "NoiseFilter": {
      "additionalProperties": true,
      "properties": {
        "cron_schedule": {
          "description": "Cron spec for noise filter downloader.",
          "type": "string"
        },
        "url": {
          "description": "URL of noise filter list (CSV format: category,rcode,domain_suffix).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ProxyProtocolConfig": {
      "additionalProperties": true,
      "properties": {
        "enabled": {
          "description": "Require PROXY protocol header for DoT connections.",
          "type": "boolean"
        },
        "trusted_proxies": {
          "description": "Comma-separated list of trusted proxy IP addresses or CIDR ranges.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ServerConfig": {
      "additionalProperties": true,
      "properties": {
        "api_keys": {
          "description": "Map of API keys to user descriptions for admin API access.",
          "patternProperties": {
            ".*": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "data_dir": {
          "description": "Directory for storing persistent data (e.g., TLS certificate cache).",
          "type": "string"
        },
        "dev_mode": {
          "description": "Run server in dev mode (no TLS, plain TCP).",
          "type": "boolean"
        },
        "dns_port": {
          "description": "The port to run regular DNS (UDP/TCP) server on.",
          "type": "integer"
        },
        "dot_port": {
          "description": "The port to run DNS-over-TLS server on.",
          "type": "integer"
        },
        "http_port": {
          "description": "The port to run HTTP server on.",
          "type": "integer"
        },
        "lets_encrypt": {
          "additionalProperties": true,
          "properties": {
            "allowed_hosts": {
              "description": "List of domains used for CertManager allow policy / mobileconfig.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "cloudflare_api_token": {
              "description": "Cloudflare API token for DNS-01 challenge.",
              "type": "string"
            },
            "email": {
              "description": "Email address for Let's Encrypt registration.",
              "type": "string"
            },
            "enabled": {
              "description": "Enable automatic TLS certificate management via Let's Encrypt / ACME.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "log_level": {
          "description": "The logging level (DEBUG, INFO, WARN, ERROR).",
          "enum": [
            "DEBUG",
            "INFO",
            "WARN",
            "ERROR"
          ],
          "type": "string"
        },
        "proxy_protocol": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "description": "Require PROXY protocol header for DoT connections.",
              "type": "boolean"
            },
            "trusted_proxies": {
              "description": "Comma-separated list of trusted proxy IP addresses or CIDR ranges.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "TelemetryConfig": {
      "additionalProperties": true,
      "properties": {
        "metrics_auth": {
          "description": "Credentials for basic auth on /metrics (format: user:pass).",
          "type": "string"
        },
        "otel_endpoint": {
          "description": "OpenTelemetry OTLP gRPC endpoint (e.g. localhost:4317).",
          "type": "string"
        },
        "otel_sampling_ratio": {
          "description": "Ratio of traces to sample (0.0 to 1.0).",
          "type": "number"
        },
        "sentry_dsn": {
          "description": "DSN for Sentry error reporting.",
          "type": "string"
        },
        "top_k": {
          "additionalProperties": true,
          "description": "Configuration for the number of top entries to track in Prometheus metrics.",
          "properties": {
            "num_blocked": {
              "description": "Number of top blocked domains to track in the dns_top_blocked_domains Prometheus metric.",
              "type": "integer"
            },
            "num_clients": {
              "description": "Number of top clients to track in the dns_top_clients Prometheus metric.",
              "type": "integer"
            },
            "num_domains": {
              "description": "Number of top (non-blocked) domains to track in the dns_top_domains Prometheus metric.",
              "type": "integer"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "TimeoutsConfig": {
      "additionalProperties": true,
      "properties": {
        "dial": {
          "description": "Timeout for establishing connections to upstream servers.",
          "format": "duration",
          "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
          "type": "string"
        },
        "read": {
          "description": "Timeout for reading upstream DNS queries.",
          "format": "duration",
          "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
          "type": "string"
        },
        "write": {
          "description": "Timeout for writing upstream DNS queries.",
          "format": "duration",
          "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
          "type": "string"
        }
      },
      "type": "object"
    },
    "TopKConfig": {
      "additionalProperties": true,
      "description": "Configuration for the number of top entries to track in Prometheus metrics.",
      "properties": {
        "num_blocked": {
          "description": "Number of top blocked domains to track in the dns_top_blocked_domains Prometheus metric.",
          "type": "integer"
        },
        "num_clients": {
          "description": "Number of top clients to track in the dns_top_clients Prometheus metric.",
          "type": "integer"
        },
        "num_domains": {
          "description": "Number of top (non-blocked) domains to track in the dns_top_domains Prometheus metric.",
          "type": "integer"
        }
      },
      "type": "object"
    }
  },
  "properties": {
    "blocklist": {
      "additionalProperties": true,
      "properties": {
        "sources": {
          "description": "Array of blocklist sources, each with its own name, URL and cron schedule.",
          "items": {
            "additionalProperties": true,
            "properties": {
              "cron_schedule": {
                "description": "Cron spec for reloading this specific blocklist. If omitted, the blocklist is not scheduled for automatic updates.",
                "type": "string"
              },
              "description": {
                "description": "Optional description for the blocklist.",
                "type": "string"
              },
              "name": {
                "description": "Human-readable name for the blocklist (replaces the auto-generated 'Blocklist #N').",
                "type": "string"
              },
              "title": {
                "description": "Optional title for the blocklist.",
                "type": "string"
              },
              "url": {
                "description": "URL of the blocklist source.",
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "dns": {
      "additionalProperties": true,
      "properties": {
        "cache": {
          "additionalProperties": true,
          "properties": {
            "cron_schedule": {
              "description": "Cron spec for cache reaper.",
              "type": "string"
            },
            "max_size": {
              "description": "Maximum number of entries in the DNS cache.",
              "type": "integer"
            },
            "ttl_floor": {
              "description": "Minimum TTL for cached entries.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            }
          },
          "type": "object"
        },
        "ecs": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "description": "Whether to enable EDNS0 Client Subnet (ECS) forwarding.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "noise_filter": {
          "additionalProperties": true,
          "properties": {
            "cron_schedule": {
              "description": "Cron spec for noise filter downloader.",
              "type": "string"
            },
            "url": {
              "description": "URL of noise filter list (CSV format: category,rcode,domain_suffix).",
              "type": "string"
            }
          },
          "type": "object"
        },
        "timeouts": {
          "additionalProperties": true,
          "properties": {
            "dial": {
              "description": "Timeout for establishing connections to upstream servers.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            "read": {
              "description": "Timeout for reading upstream DNS queries.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            },
            "write": {
              "description": "Timeout for writing upstream DNS queries.",
              "format": "duration",
              "pattern": "^([0-9]+(?:\\.[0-9]+)?(?:ns|us|µs|ms|s|m|h))+$",
              "type": "string"
            }
          },
          "type": "object"
        },
        "upstreams": {
          "description": "Upstream DNS resolvers to forward queries to.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "geoblock": {
      "additionalProperties": true,
      "properties": {
        "ipinfo": {
          "additionalProperties": true,
          "properties": {
            "cron_schedule": {
              "description": "Cron spec for Ipinfo.io database downloader.",
              "type": "string"
            },
            "enabled": {
              "description": "Whether to enable IPinfo.io geolocation lookups.",
              "type": "boolean"
            },
            "token": {
              "description": "IPInfo.io API token for downloading geoIP locations.",
              "type": "string"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "server": {
      "additionalProperties": true,
      "properties": {
        "api_keys": {
          "description": "Map of API keys to user descriptions for admin API access.",
          "patternProperties": {
            ".*": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "data_dir": {
          "description": "Directory for storing persistent data (e.g., TLS certificate cache).",
          "type": "string"
        },
        "dev_mode": {
          "description": "Run server in dev mode (no TLS, plain TCP).",
          "type": "boolean"
        },
        "dns_port": {
          "description": "The port to run regular DNS (UDP/TCP) server on.",
          "type": "integer"
        },
        "dot_port": {
          "description": "The port to run DNS-over-TLS server on.",
          "type": "integer"
        },
        "http_port": {
          "description": "The port to run HTTP server on.",
          "type": "integer"
        },
        "lets_encrypt": {
          "additionalProperties": true,
          "properties": {
            "allowed_hosts": {
              "description": "List of domains used for CertManager allow policy / mobileconfig.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "cloudflare_api_token": {
              "description": "Cloudflare API token for DNS-01 challenge.",
              "type": "string"
            },
            "email": {
              "description": "Email address for Let's Encrypt registration.",
              "type": "string"
            },
            "enabled": {
              "description": "Enable automatic TLS certificate management via Let's Encrypt / ACME.",
              "type": "boolean"
            }
          },
          "type": "object"
        },
        "log_level": {
          "description": "The logging level (DEBUG, INFO, WARN, ERROR).",
          "enum": [
            "DEBUG",
            "INFO",
            "WARN",
            "ERROR"
          ],
          "type": "string"
        },
        "proxy_protocol": {
          "additionalProperties": true,
          "properties": {
            "enabled": {
              "description": "Require PROXY protocol header for DoT connections.",
              "type": "boolean"
            },
            "trusted_proxies": {
              "description": "Comma-separated list of trusted proxy IP addresses or CIDR ranges.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "telemetry": {
      "additionalProperties": true,
      "properties": {
        "metrics_auth": {
          "description": "Credentials for basic auth on /metrics (format: user:pass).",
          "type": "string"
        },
        "otel_endpoint": {
          "description": "OpenTelemetry OTLP gRPC endpoint (e.g. localhost:4317).",
          "type": "string"
        },
        "otel_sampling_ratio": {
          "description": "Ratio of traces to sample (0.0 to 1.0).",
          "type": "number"
        },
        "sentry_dsn": {
          "description": "DSN for Sentry error reporting.",
          "type": "string"
        },
        "top_k": {
          "additionalProperties": true,
          "description": "Configuration for the number of top entries to track in Prometheus metrics.",
          "properties": {
            "num_blocked": {
              "description": "Number of top blocked domains to track in the dns_top_blocked_domains Prometheus metric.",
              "type": "integer"
            },
            "num_clients": {
              "description": "Number of top clients to track in the dns_top_clients Prometheus metric.",
              "type": "integer"
            },
            "num_domains": {
              "description": "Number of top (non-blocked) domains to track in the dns_top_domains Prometheus metric.",
              "type": "integer"
            }
          },
          "type": "object"
        }
      },
      "type": "object"
    }
  },
  "type": "object"
}